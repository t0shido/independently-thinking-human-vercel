/**
 * Static-site configuration for the Independently Thinking Human project.
 */
const config = {
  content: {
    sections: ['mindset', 'politics', 'economics', 'technology', 'stories'],
    mediaUrl: '/library'
  }
};

export default config;
